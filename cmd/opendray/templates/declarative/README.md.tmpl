# {{.DisplayName}}

A declarative OpenDray plugin scaffolded by `opendray plugin scaffold`.

## Try it

```sh
opendray plugin validate ./{{.Name}}
OPENDRAY_ALLOW_LOCAL_PLUGINS=1 opendray plugin install ./{{.Name}}
```

Press `Ctrl+Alt+H` (or `Cmd+Alt+H` on Mac), or click the 🔌 chip in the status bar.
