project_name: opendray
before:
  hooks:
    - cd app && flutter pub get && flutter build web --release

builds:
  - id: opendray
    main: ./cmd/opendray
    binary: opendray
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.buildSha={{.ShortCommit}}

archives:
  - id: opendray
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

# Generate SHA256SUMS alongside archives so install.sh can verify
# download integrity. Cosign-signed in the Release workflow via
# `cosign sign-blob --yes dist/checksums.txt`.
checksum:
  name_template: 'SHA256SUMS'
  algorithm: sha256

release:
  github:
    owner: opendray
    name: opendray

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^chore:'
