name: DCO

on:
  pull_request:
    branches: [main]

jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
      - uses: tim-actions/dco@v1.1.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
